/**
 * The four columns of the routine sheet — movement, sets, reps, load — shared by
 * the header and every row under it. One definition, because a header that
 * drifts from its rows stops being a table and starts being four lists.
 */
export const SHEET_GRID = 'grid grid-cols-[1fr_1.75rem_2rem_2.75rem] items-center gap-1.5';
